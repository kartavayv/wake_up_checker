name: Wake Up Script

on:
  schedule:
    - cron: '45 21 * * *'  # This is 3:15 AM IST (UTC+5:30)
  workflow_dispatch:

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set token
        run: echo "DISCORD_TOKEN=${{ secrets.DISCORD_TOKEN }}" >> $GITHUB_ENV

      - name: Run script
        run: python wake_up_checker.py
